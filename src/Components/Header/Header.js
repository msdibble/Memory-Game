import React from "react";
import "./Header.css";

const Header = props => (
    <header className = "header">
        <h1>Memory Game</h1>
        <h2>Click on any image below to start the game and start earning points. Be sure to not click on an image more than once!</h2>
    </header>
);

export default Header;